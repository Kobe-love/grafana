interface:
  display_name: "Grafana"
  short_description: "Repo-specific patterns and workflows for grafana"
  default_prompt: "Use the grafana repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true